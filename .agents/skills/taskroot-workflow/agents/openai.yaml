interface:
  display_name: "Taskroot Workflow"
  short_description: "Drive Filer work through validated tasks"
  default_prompt: "Use $taskroot-workflow to plan and execute this Filer change through its task lifecycle."
