# Large Directory Baseline, 2026-09-04

This baseline measures commit `1bfb08a` through the public `Command::Scan`,
`Event::DirectoryPageLoaded`, `Command::Extension`, and
`Event::FileDecorationsUpdated` contracts after PIPELINE-003 landed.

## Machine Profile

- OS: Linux 6.12.101+deb13-amd64, x86_64
- CPU: Intel Core i7-11800H, 16 logical CPUs
- Rust: rustc 1.97.1 (LLVM 22.1.6)
- Cargo: 1.97.1
- Filesystem: Btrfs on NVMe, compression zstd level 1
- Fixture: 10,000 empty files in a directory inside an isolated Git worktree
- Page size: 256
- Sampling: 3 warmups, 20 timed samples per scenario

The command used a fixture root on Btrfs instead of the system temporary
filesystem:

```bash
FILER_BENCH_FIXTURE_ROOT=target/bench-fixtures cargo bench -p filer-core --bench large_directory
```

Fixture generation took 83.509 ms and was excluded from every timed sample.
The decoration scenario waits for its `FileDecorationsUpdated` event after the
listing sample ends, so Git completion time is not included in listing latency.

## Results

| Scenario | Rows emitted | Minimum | Median | p95 | Maximum | Mean |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Fast first page, decorations off | 256 | 0.269 ms | 0.272 ms | 0.301 ms | 0.309 ms | 0.280 ms |
| Fast first page, decorations on | 256 | 0.448 ms | 1.033 ms | 1.348 ms | 1.366 ms | 0.957 ms |
| Fast next page | 256 | 0.239 ms | 0.250 ms | 0.336 ms | 0.426 ms | 0.266 ms |
| Metadata first page | 256 | 1.912 ms | 1.953 ms | 2.094 ms | 2.106 ms | 1.983 ms |
| Sorted first page | 256 | 87.806 ms | 88.989 ms | 91.102 ms | 91.409 ms | 89.131 ms |
| Fast full snapshot | 10,000 | 8.042 ms | 8.486 ms | 8.968 ms | 9.058 ms | 8.453 ms |

## Gate Status

The public structural paging test passes. Its controllable stream yielded 257
of 10,000 rows, did not reach the directory end, and emitted the first 256-row
page with a continuation cursor.

The decoration ordering test passes with the backend held open: the first
directory page arrives before the real Git decoration work is released. The
active-decoration benchmark reports listing latency alongside the decorations-
off comparison. On this run, the active request's p95 was 1.348 ms versus
0.301 ms without active work; the timing is comparison evidence for this
machine, while event ordering is the portable non-blocking gate.

The sorted scenario remains snapshot-bound and therefore costs a full walk. The
absolute timings are not cross-machine thresholds. Repeat the command with the
same machine, filesystem, toolchain, entry count, and page size when comparing
future revisions.
