use std::path::Path;

/// Get file extension as lowercase string
pub fn get_extension(path: &Path) -> Option<String> {
    todo!()
}

/// Get file stem (name without extension)
pub fn get_stem(path: &Path) -> Option<String> {
    todo!()
}

/// Check if path is hidden (starts with dot on Unix)
pub fn is_hidden(path: &Path) -> bool {
    todo!()
}

/// Normalize path separators
pub fn normalize(path: &Path) -> std::path::PathBuf {
    todo!()
}

/// Get parent directory name
pub fn parent_name(path: &Path) -> Option<String> {
    todo!()
}
